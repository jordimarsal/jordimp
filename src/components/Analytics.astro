---
import { goatCounterEndpoint } from '../lib/analytics';

const endpoint = goatCounterEndpoint(import.meta.env.PUBLIC_GOATCOUNTER);
---
{endpoint && <script is:inline async data-goatcounter={endpoint} src="https://gc.zgo.at/count.js"></script>}
