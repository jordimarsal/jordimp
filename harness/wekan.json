{
  "_readme": "Wekan ticket mirror config (no secrets here). board_name empty means: use the project name from harness/feature_list.json. credentials_file points to a gitignored env file that must define the Wekan API bearer token and user id (exact names in SKILL.md). enabled=false is a kill switch: agents skip all Wekan actions.",
  "url": "https://wekan.example.test",
  "board_name": "",
  "list_map": {
    "pending": "pending",
    "spec_ready": "spec_ready",
    "in_progress": "in_progress",
    "blocked": "blocked",
    "done": "done"
  },
  "credentials_file": ".harness-wekan.env",
  "enabled": true
}
